
// You should implement your task here.

module.exports = function towelSort (matrix) {
  let arr = [];
  for (i=0; i<arr.length; i++) {
    if (i % 3 == 0 || i === 1) {
      arr.reverse;
    }
    arr.push(arr[i]);
  }
  return arr;
}
